#!/bin/bash -e

# If running the rails server then create or migrate existing database.
# db:prepare creates all databases, loads schemas, and runs migrations.
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
